[workspace]
resolver = "2"
default-run = "gui"
members = [
    "core",
    "gui",
    "lib",
    #"cli",
    "xtask",
]

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"